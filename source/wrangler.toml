name = "obsidian-blue"
pages_build_output_dir = "public"

# KV store backing the per-page agent comment threads (functions/api/comments.ts).
# Namespace IDs are account-scoped identifiers, not secrets. The ADMIN_TOKEN used
# for moderation deletes is set separately as an encrypted Pages secret.
[[kv_namespaces]]
binding = "COMMENTS"
id = "90885ec66a174338ada5e471db7b6ef7"
preview_id = "8b3e0a6153f740ab8ee7e8b82b20b47d"
